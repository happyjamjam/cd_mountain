<h1>Cards#new</h1>

<%= form_tag (new_card_path, method: :post, id: 'charge-form', name: "inputForm") do |f| %>
	<label> カード番号</label>
	<%= f.text_field_tag "number", "", class: "number", placeholder: "半角数字のみ", maxlength: "16", type: "text", id: "card_number" %>
	<br>
	<label> 有効期限</label>
	<%= f.select
end