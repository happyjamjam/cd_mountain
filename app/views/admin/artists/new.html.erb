<%= form_for @artist, url: admin_artists_path do |f| %>

	<div class="field">
		<%= f.label :アーティスト名 %>
		<%= f.text_field :artist_name %>
	</div>

	<div class="field">
		<%= f.label :ひらがな %>
		<%= f.text_field :artist_hiragana %>
	</div>

	<%= f.submit %>
<% end %>
