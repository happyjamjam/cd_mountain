<div class = "container">
  <div class = "row">
    <h2>お買い上げありがとうございました</h2>
    <p> 今回の</p><%= link_to '注文詳細', order_path(@order) %><p>はこちらから</p>
    <table class = "table">
      <tbody>
        <% @user_carts.each do |order| %>
         <tr>
         <td class = "contents"><%# attachment_image_tag order.product, :jacket_img, :fill, 100, 100, format: 'jpeg', fallback: "no_img.jpg", size:'100x100' %></td>
         <td class = "contents"><%# link_to order.product_name, product_path(product.id), class: "text-muted" %></td>
         <td class = "contents"><%= order.product.product_name %></td>
         <td class = "content" ><%= order.product.price %></td>
         <td class = "contents"><%= order.quantity %></td>
       </tr>
      <% end %>
      </tbody>
    </table>
    </div>
  </div>
</div>